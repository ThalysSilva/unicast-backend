# API
API_PORT=8070

# Auth/JWE (use random secrets; JWE must be 32-byte hex)
ACCESS_TOKEN_SECRET=changeme_access_secret
REFRESH_TOKEN_SECRET=changeme_refresh_secret
JWE_SECRET=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
TOKEN_EXPIRATION_TIME=1h
REFRESH_TOKEN_EXPIRATION_TIME=7d
ADMIN_SECRET=changeme_admin_secret

# CORS / URLs
BASE_URL=http://localhost:8070/
ALLOWED_URLS=http://localhost:3000

# Database (Postgres)
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_USER=root
POSTGRES_PASSWORD=changeme
POSTGRES_DB=unicast
POSTGRES_DATABASE_URL=postgres://root:changeme@localhost:5433/unicast?sslmode=disable

# Mongo (se usar Evolution com Mongo)
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=changeme
MONGO_DATABASE_NAME=unicast
MONGO_DATABASE_URL=mongodb://root:changeme@localhost:27017

# Evolution / WhatsApp
EVOLUTION_PORT=8080
EVOLUTION_HOST=localhost
AUTHENTICATION_API_KEY=changeme_evolution_key

# Redis
REDIS_PORT=6380
CACHE_REDIS_URI=redis://localhost:6380/6
CACHE_REDIS_PREFIX_KEY=evolution
CACHE_REDIS_ENABLED=true
CACHE_REDIS_SAVE_INSTANCES=false
CACHE_LOCAL_ENABLED=false

# PgAdmin
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=changeme
